# 常用命令

```shell

cat > /etc/apt/sources.list.d/debian.sources << EOF
Types: deb
URIs: https://mirrors.tuna.tsinghua.edu.cn/debian
Suites: trixie trixie-updates trixie-backports
Components: main contrib non-free non-free-firmware
Signed-By: /usr/share/keyrings/debian-archive-keyring.gpg

Types: deb
URIs: https://mirrors.tuna.tsinghua.edu.cn/debian-security
Suites: trixie-security
Components: main contrib non-free non-free-firmware
Signed-By: /usr/share/keyrings/debian-archive-keyring.gpg

EOF

apt-get clean all
apt-get update


apt-get install -y vim net-tools lrzsz tmux build-essential cryptsetup gpg

cryptsetup luksOpen /dev/sda my_encrypted_disk


echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/debian \
  $(. /etc/os-release && echo "$VERSION_CODENAME") stable" | \
  tee /etc/apt/sources.list.d/docker.list > /dev/null


curl -fsSL https://download.docker.com/linux/debian/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg


apt-get update

apt-get install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin


apt-get install -y inxi
inxi -Ffxxxz


apt-get install -y xorgxrdp xrdp




mount /dev/mmcblk0p2 /boot
systemctl daemon-reload

scp -r 192.168.66.188:/rockchip/Liontron-D3588.git/d3588-kernel-5.10.198.git/arch/arm64/boot/Image .

cp Image /boot/vmlinuz-5.10.198-kdev
sync
sleep 1
reboot




apt install -y qemu-system-arm qemu-kvm libvirt-daemon-system virtinst cpu-checker qemu-efi-aarch64 \
  libvirt-clients virt-manager ipxe-qemu
  
qemu-img create -f qcow2 /var/lib/libvirt/images/empty-arm-disk.qcow2 8G

 virt-install \
  --name test-vm \
  --ram 1024 \
  --vcpus 2 \
  --disk /var/lib/libvirt/images/empty-arm-disk.qcow2,format=qcow2 \
  --os-variant generic \
  --import \
   --boot uefi \
  --virt-type kvm \
  --graphics none \
  --console pty,target_type=serial \
  --noautoconsole



virt-install \
    --name debian-trixie-vm \
    --ram 2048 \
    --vcpus 2 \
    --disk path=/var/lib/libvirt/images/debian-trixie.img,size=20 \
    --os-type linux \
    --os-variant debian12 \
    --network bridge=virbr0,model=virtio \
    --graphics none \
    --virt-type=qemu \
    --machine virt \
    --boot menu=on \
    --check all=off \
    --extra-args "console=ttyAMA0 loglevel=10 level=10 debug" \
    --console pty,target_type=serial \
    --location 'http://mirrors.tencent.com/debian/dists/trixie/main/installer-arm64/'
    




virsh destroy debian-trixie-vm
virsh undefine debian-trixie-vm --nvram
virt-install \
    --name debian-trixie-vm \
    --ram 2048 \
    --vcpus 2 \
    --disk path=/var/lib/libvirt/images/debian-trixie.img,size=20 \
    --os-type linux \
    --os-variant debian12 \
    --network bridge=virbr0,model=virtio \
    --graphics none \
    --virt-type=kvm --hvm \
    --machine virt \
    --boot menu=on \
    --check all=off \
    --extra-args "console=tty loglevel=10 level=10 debug" \
    --console pty,target_type=serial \
    --location 'http://mirrors.tencent.com/debian/dists/trixie/main/installer-arm64/'
    



```